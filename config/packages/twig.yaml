twig:
    default_path: '%kernel.project_dir%/templates'
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        books_directory: '/uploads/books'
        covers_directory: '/uploads/covers'
